function hello () {
    return true ;
}

var bye = function() {
    if(hey){
        // Next line has a trailing whitespace
        return;  
    }  
};

try {
    var other = {
        a: true ,
        b :false
    };
}    catch    ( ex )    { // no errors thrown
    if ( ex ) {           // errors
    }
}
